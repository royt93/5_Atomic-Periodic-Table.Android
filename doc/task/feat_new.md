# Feature mới — task breakdown (2026-09-01)

Chọn qua AskUserQuestion sau Sprint 3 + ponytail-audit. User chọn "cả 4 option đều tốt" — rã task để loop lần lượt.

## 1. Molar Mass / Stoichiometry Calculator — ✅ Đã có sẵn (không phải feature mới)

Khảo sát phát hiện feature này **đã được implement từ trước**, không nằm trong 4 lựa chọn thực sự cần làm:
- Parser: `util/ChemicalFormulaParser.kt` (`parse(formula): Map<String, Int>`, xử lý ngoặc lồng nhau + hệ số nhân), có test JVM đầy đủ `ChemicalParserAndBalancerTest.kt`.
- UI: `act/CalculatorAct.kt:67` — nhập công thức, tính tổng khối lượng qua `ElementWeightCache.getMass(symbol)`.
- Entry point: `menuMolarMassBtn` trong nav menu → `MainAct.kt:453`.

→ Không cần làm lại. 3 feature còn lại tiến hành theo thứ tự effort tăng dần:

## 2. Element of the Day (widget) — ✅ Đã fix (2026-09-01)

- Pick nguyên tố theo ngày: hàm pure `pickElementOfDay(dateEpochDay: Long, totalElements: Int): Int` (index theo `dateEpochDay % totalElements`, deterministic, unit-testable JVM không cần Android).
- Cần 1 dòng "fact" mỗi nguyên tố — tái dùng dữ liệu JSON asset đã có (`assets/<element>.json`) qua field có sẵn (kiểm tra field nào hợp lý, ví dụ description/category) thay vì tạo dữ liệu fact mới.
- Widget: mở rộng `widget/ShortCommandWidget.kt` (đã có `onUpdate` + `updatePeriodMillis=86400000` = 24h sẵn) — thêm hiển thị tên/ký hiệu/fact nguyên tố hôm nay vào `RemoteViews`, tap mở `ElementInfoAct` đúng nguyên tố đó (không phải mở `MainAct` chung chung như hiện tại).
- Không thêm WorkManager/AlarmManager mới — `updatePeriodMillis` có sẵn đã đủ (ponytail: platform feature đã có, không thêm dependency).
- Test: JVM unit test cho `pickElementOfDay` (deterministic, đổi ngày → đổi index, index luôn trong `0 until totalElements`).
- **Đã implement:** `util/ElementOfDay.kt` (`indexForDay(epochDay, total)`, dùng `System.currentTimeMillis() / 86_400_000L` thay `java.time.LocalDate` vì minSdk 24 < API 26). `ElementWeightCache` mở rộng thêm `descriptionCache`/`getFact()` (câu đầu tiên của field `description` có sẵn trong JSON asset). `ShortCommandWidget.onUpdate()` set `tvWidgetName`/`tvWidgetFact` qua `RemoteViews`, ghi `ElementSendAndLoad` rồi trỏ click PendingIntent sang `ElementInfoAct` (trước đây mở `MainAct` chung chung). Cả 2 layout variant (`layout/`, `layout-v31/`) đều khai báo 2 id mới.
- Test: `ElementOfDayTest` (4 case JVM), `ShortCommandWidgetLayoutTest` mở rộng để check cả `setTextViewText` id (không chỉ `setOnClickPendingIntent`) tồn tại ở mọi layout variant, `ShortCommandWidgetTest.onUpdate_pointsElementSendAndLoad_atTodaysElement` (instrumented) — verify bằng revert-test: comment dòng `setValue` → test fail đúng như kỳ vọng, khôi phục → pass. 3/3 instrumented pass trên TECNO KJ7.
- Trạng thái: ✅ Đã fix

## 3. Flashcard / Spaced-repetition study — ✅ Đã fix (2026-09-01)

- Thuật toán SM-2 rút gọn, pure object tương tự `VipCalculator` (JVM-testable, Android-independent): input rating (Again/Hard/Good/Easy) + state hiện tại (easeFactor, intervalDays, repetitions) → state mới.
- Persist theo từng nguyên tố bằng pattern `NotesPref` (`pref/NotesPref.kt`) — key `"flashcard_next_$symbol"` (Long, epoch ms), `"flashcard_interval_$symbol"` (Int), `"flashcard_ease_$symbol"` (Float) — không cần Map/JSON phức tạp, đúng pattern đã có trong codebase.
- UI: `act/FlashcardAct.kt` mới — mặt trước ký hiệu, tap lật mặt sau (tên + số nguyên tử), 4 nút rating. Tham khảo animation pattern từ `QuizAct.kt`.
- Entry point: nav menu (`view_nav_menu_view.xml` + `MainAct.kt` wiring), theo đúng pattern "Compare Elements" đã ghi trong `doc/quick_win.md`.
- Test: JVM unit test cho thuật toán SM-2 rút gọn (input/output rõ ràng, giống `VipCalculatorTest`).
- **Đã implement:** `feature/flashcard/FlashcardScheduler.kt` (pure, SM-2 rút gọn — AGAIN reset về 1 ngày + giảm ease, HARD/GOOD/EASY tăng interval theo ease factor với mốc khởi đầu khác nhau), `feature/flashcard/FlashcardPref.kt` (flat key theo symbol, đúng pattern `NotesPref`), `act/FlashcardAct.kt` (queue = nguyên tố đến hạn `isDue`, rơi về full deck nếu chưa nguyên tố nào đến hạn — practice mode), entry point nav menu (`menuFlashcardBtn` cạnh `menuQuizBtn`).
- UI Material You: `MaterialCardView`/`CardView` tông màu `colorPrimaryContainer`, `LinearProgressIndicator` animated (`setProgressCompat`), 4 nút rating dùng `MaterialButton` với `style=` **explicit** (`Widget.App.MaterialButton`/`.Outlined`) — bài học thật: bỏ sót `style=` cho 2 nút khiến chúng rơi vào default MDC (sai màu + ALL CAPS), phát hiện qua screenshot thật trên device (không chỉ đọc code). Animation: flip 3D thật (`ObjectAnimator` xoay `rotationY` 2 chặn, swap mặt trước/sau ở giữa) + slide-fade khi chuyển thẻ.
- Bug thật bắt được bởi test trước khi push: `cardFlashcardComplete` ban đầu constrain theo cạnh `cardFlashcard`, nhưng `cardFlashcard` bị set GONE khi hết thẻ → bounds sụp về 0, complete-card thành invisible. Sửa bằng cách cho `cardFlashcardComplete` constraint độc lập giống hệt `cardFlashcard` (neo vào `flashcardProgressBar`/`layoutFlashcardRatings`, không neo vào nhau).
- Test: `FlashcardSchedulerTest` (17 case JVM, cover đủ 4 rating × biên ease-factor/interval), `FlashcardPrefTest` (3 case instrumented: default/roundtrip/isolation theo symbol), `FlashcardActTest` (6 case instrumented: launch/flip/rating-Good/rating-Again/ignore-trước-flip/edge-case-practice-mode/integration hết-bộ-thẻ-118-lần), `NavigationIntegrationTest.testNavigateFromMainToFlashcard`. Verify: 7/7 + 3/3 + 4/4 pass trên emulator sạch (Pixel 10 Pro XL AVD) sau khi loại trừ 2 lần nhiễu môi trường (TECNO KJ7 bị `com.galaxyjoy.cpuinfo` cướp focus giữa test 118-vòng — xác nhận qua `dumpsys window`; 1 lần "Process crashed" do harness gộp nhiều test class uninstall app giữa chừng — xác nhận qua logcat `pm uninstall`). Full unit suite xanh.
- Trạng thái: ✅ Đã fix

## 4. Periodic Trends Chart — ✅ Đã fix (2026-09-01)

- Custom View tự vẽ Canvas (tham khảo cấu trúc `view/ConfettiView.kt` — Paint/Canvas/Path), KHÔNG thêm thư viện chart mới (ladder: platform feature đã đủ cho line/scatter đơn giản).
- Chọn property hiển thị: electronegativity (`Element.electro` có sẵn trong model) là lựa chọn an toàn nhất vì đã có sẵn trong `Element` data class, không cần tra JSON asset cho toàn bộ 118 phần tử. Nếu muốn nhiều property hơn (atomic radius, ionization energy...) phải đọc JSON asset của cả 118 file — cân nhắc effort trước khi mở rộng, có thể để iteration sau.
- Vẽ trục X = atomic number (1-118), trục Y = giá trị property, chấm/line nối các điểm có dữ liệu (một số nguyên tố `electro == 0.0` = không có dữ liệu, phải bỏ qua điểm đó khi vẽ, không vẽ giá trị 0 giả).
- Tap vào 1 điểm hiện tooltip tên nguyên tố + giá trị (dùng toạ độ chạm so khớp điểm gần nhất, tương tự cách `NuclideAct` xử lý touch).
- Test: JVM unit test cho hàm mapping toạ độ dữ liệu → toạ độ pixel (pure function, tách khỏi View để test không cần Android).
- **Đã implement:** `feature/trends/TrendsMapper.kt` (pure: `mapX`/`mapY`/`nearestPointIndex`), `view/TrendsChartView.kt` (Canvas tự vẽ, tham khảo `ConfettiView`), `act/TrendsChartAct.kt` + `a_trends_chart.xml` (MaterialCardView tông `colorSurfaceVariant`, tooltip text dưới chart), entry point nav menu (`menuTrendsBtn`). Property hiển thị: electronegativity (`Element.electro`, có sẵn trong model, không cần đọc JSON). Bỏ qua điểm `electro == 0.0` khi vẽ (đúng yêu cầu, không vẽ giá trị 0 giả).
- Test: `TrendsMapperTest` (9 case JVM: biên min/max X/Y, đảo chiều Y, nearest-point kể cả list rỗng), `TrendsChartActTest` (4 case instrumented: hiện chart+hint, tap đúng điểm đã biết → tooltip đúng ký hiệu nguyên tố, tap xa mọi điểm → tooltip không đổi, tap 2 điểm khác nhau → tooltip cập nhật đúng lần sau), `NavigationIntegrationTest.testNavigateFromMainToTrendsChart`. Verify bằng screenshot thật (base64-log-in-logcat) trên emulator sạch — xác nhận UI Material You đúng chuẩn (card tông màu, dot/line màu `colorPrimary`, điểm được chọn nổi bật màu `colorTertiary`, tooltip hiển thị đúng "Xe — Xenon: 2.60"), không phải đọc code suông. 9/9 + 4/4 + 5/5 (cả bộ NavigationIntegrationTest) pass trên Pixel 10 Pro XL AVD. Full unit suite xanh.
- Trạng thái: ✅ Đã fix

---

# Vòng 2 (2026-09-02) — 4 feature mới chọn qua AskUserQuestion

User chọn "cả 4 idea đều hay" sau khi được đề xuất Unit Converter mở rộng (Recommended)/Share ảnh/Study Streak/Practice Exam. Rã task để loop lần lượt, theo đúng effort tăng dần và quy tắc chung ở cuối file.

## 5. Unit Converter (công cụ đổi đơn vị hoá học) — ✅ Đã fix (2026-09-02)

- Khảo sát: `pref/TemperatureUnits.kt:7-23` chỉ là pref lưu đơn vị **hiển thị** (1 key, 3 giá trị celsius/kelvin/fahrenheit) cho các màn hình đã có sẵn số liệu theo Kelvin trong JSON — không phải máy tính đổi qua lại. Field `element_density` trong JSON asset là string nhúng đơn vị (`"0.0000899 (g/cm^3)"`), không phải số thuần, và không có field áp suất/thể tích mol nào trong data — nghĩa là **không thể** làm kiểu "toggle hiển thị" như nhiệt độ cho áp suất/khối lượng.
- Quyết định phạm vi: làm 1 **công cụ đổi đơn vị độc lập** (nhập giá trị + chọn đơn vị nguồn/đích + tính), không phải gắn với dữ liệu nguyên tố. Công thức đổi (atm↔kPa↔mmHg↔psi cho áp suất; g↔kg↔mg cho khối lượng; L↔mL cho thể tích) là hằng số toán học cố định, không cần đọc JSON.
- UI: `act/UnitConverterAct.kt` mới — 1 màn hình dùng chung cho cả 3 nhóm đơn vị (chip chọn nhóm Pressure/Mass/Volume, rồi 2 dropdown/chip from-unit và to-unit, EditText nhập giá trị, TextView kết quả). Tham khảo layout pattern `a_calculator.xml`/`CalculatorAct.kt` (input → validate → hiển thị kết quả qua `TextInputLayout`).
- Logic pure: `feature/converter/UnitConverter.kt` — object với `convertPressure(value, from, to): Double`, `convertMass(...)`, `convertVolume(...)`, mỗi hàm quy đổi qua 1 đơn vị gốc trung gian (ví dụ Pascal cho áp suất) rồi ra đơn vị đích — JVM-testable thuần, không cần Context.
- Không cần SharedPreferences mới (không có gì persist — mỗi lần mở lại reset, giống Calculator/EquationBalancer đã có).
- Entry point: nav menu (`menuUnitConverterBtn` theo đúng pattern các mục trước).
- Test: JVM unit test cho `UnitConverter` (mọi cặp đơn vị, giá trị biên 0, giá trị âm cho áp suất nên reject hay cho qua — cần quyết định khi code, không đoán).
- **Quyết định giá trị âm:** reject (throw `IllegalArgumentException`) cho cả 3 nhóm — áp suất/khối lượng/thể tích là đại lượng vật lý luôn dương trong ngữ cảnh hoá học phổ thông (khác nhiệt độ có thang âm hợp lệ), không cần hỏi user vì lý do miền giá trị rõ ràng.
- **Đã implement:** `feature/converter/UnitConverter.kt` (3 enum `PressureUnit`/`MassUnit`/`VolumeUnit` mang theo hệ số quy đổi về 1 đơn vị gốc + object `UnitConverter` với 3 hàm convert, `require(value >= 0)` reject âm), `act/UnitConverterAct.kt` + `a_unit_converter.xml` — `ChipGroup` singleSelection chọn nhóm đơn vị (Material `Chip`, không tự chế toggle), 2 `TextInputLayout.ExposedDropdownMenu` (from/to unit) dùng `AutoCompleteTextView`, nút swap ⇄. Entry point nav menu (`menuUnitConverterBtn`).
- **Bug thật bắt được trước khi push (tự audit, không phải test bắt):** dropdown adapter ban đầu dùng `android.R.layout.simple_list_item_1` (layout Android gốc, không theo theme Material của app — tiềm ẩn vỡ màu ở dark mode). Sửa sang `androidx.appcompat.R.layout.support_simple_spinner_dropdown_item` (layout chuẩn cho `AutoCompleteTextView`/`ExposedDropdownMenu`, tự theo `?attr` màu theme). Verify bằng screenshot thật chụp **màn hình thật** (`UiAutomation.takeScreenshot()`, khác kỹ thuật `View.draw()` cũ vì PopupWindow của dropdown không nằm trong decorView của Activity nên `drawToBitmap` không chụp được popup).
- Test: `UnitConverterTest` (16 case JVM: mọi cặp đơn vị đại diện, roundtrip, biên 0, âm throw cho cả 3 nhóm — revert-tested bằng cách đổi sai hệ số PSI, confirm fail, khôi phục, confirm pass), `UnitConverterActTest` (7 case instrumented: mặc định Pressure atm→kPa, chuyển Mass/Volume category, swap, âm hiện lỗi đúng message không crash, rỗng không lỗi, chuyển category sau khi swap phải reset về from/to mặc định chứ không giữ index đã swap), `NavigationIntegrationTest.testNavigateFromMainToUnitConverter`. 16/16 + 7/7 + 1/1 pass — smoke test S24U (ưu tiên theo yêu cầu, dù `com.galaxyjoy.cpuinfo` từ session khác đang chiếm focus lúc chạy, verify bằng dumpsys không ảnh hưởng kết quả) + emulator sạch. Full unit suite xanh.
- Trạng thái: ✅ Đã fix

## 6. Chia sẻ thẻ nguyên tố dưới dạng ảnh — ✅ Đã fix (2026-09-02)

- Khảo sát: codebase đã có share text (`ext/Activity.kt:89-103`, `shareApp()`) nhưng **chưa có share ảnh** — không có `FileProvider`, không có `Bitmap`/`drawToBitmap` ở đâu (grep toàn repo ra rỗng). Đây là feature mới hoàn toàn về mặt hạ tầng.
- Ladder: dùng thẳng `View.drawToBitmap()` (extension có sẵn trong `androidx-core-ktx`, đã là dependency của project) trên 1 layout card riêng dựng cho mục đích share — KHÔNG tự vẽ Canvas thủ công như `ConfettiView`/`TrendsChartView` (không cần thiết, chỉ là export 1 layout tĩnh).
- Cần khai báo `FileProvider` mới trong `AndroidManifest.xml` (`res/xml/file_paths.xml`) để share file ảnh ra ngoài app — kiểm tra kỹ quyền `authorities` trùng `applicationId` để tránh crash `FileUriExposedException`.
- UI card share: layout riêng nhỏ gọn (symbol lớn, tên, số nguyên tử, khối lượng, category) lấy data từ `model/Element` (đã có sẵn qua `ElementModel.getList()`, không cần đọc JSON) — đặt nút "Share" trong `ElementInfoAct.kt`.
- Luồng: render layout ẩn (không add vào UI thấy được, hoặc render rồi `drawToBitmap()` ngay) → lưu vào `cacheDir` (dùng `context.cacheDir`, tự dọn được, không cần quyền storage) → `FileProvider.getUriForFile()` → `Intent.ACTION_SEND` với `type = "image/png"` + `EXTRA_STREAM` → `createChooser()` (theo đúng try/catch pattern của `shareApp()`).
- Test: instrumented test verify bitmap tạo ra không rỗng (width/height > 0) và file tồn tại sau khi gọi hàm share; JVM test không khả thi cho phần vẽ (cần Android Bitmap API) — ghi rõ lý do trong doc nếu quyết định vậy khi code.
- **Đã implement:** `feature/share/ElementCardRenderer.kt` (inflate `view_element_share_card.xml` không gắn UI, `measure()`/`layout()` thủ công ở kích thước cố định 360dp rồi `drawToBitmap()` — không tự vẽ Canvas). `ext/Activity.kt` thêm `Activity.shareImage(bitmap, fileName)` (mirror pattern `shareApp()`: ghi `cacheDir`, `FileProvider.getUriForFile()`, `ACTION_SEND` type `image/png`, try/catch). `res/xml/file_paths.xml` + `<provider>` FileProvider mới trong Manifest (`authorities="${applicationId}.fileprovider"`). `ElementInfoAct.shareCurrentElement()` lấy symbol/số/tên qua `ElementModel`+`ElementTranslator`, khối lượng/category qua `ElementWeightCache` (tái dùng cache đã có, không đọc JSON thêm). Nút share (`ic_baseline_ios_share_24` có sẵn) đặt cạnh nút Wikipedia trong title bar — bọc 2 icon trong 1 `LinearLayoutCompat` gravity end, tăng `elementTitle` marginEnd 70dp→126dp để không đè chữ.
- **Bug thật bắt được và fix trước khi push:** test ban đầu gọi `ElementCardRenderer.render()` với `ApplicationProvider.getApplicationContext()` (context trần không theme) → `InflateException` thật vì layout tham chiếu `?attr/colorPrimaryContainer` (chỉ resolve được trên context có theme). Sửa test dùng context từ 1 Activity thật đã launch (`ActivityScenario`) — đúng với cách production code luôn gọi (`this` từ `ElementInfoAct`, đã có theme).
- Test: `ElementCardRendererTest` (2 case instrumented: kích thước bitmap đúng, có vẽ nội dung thật không phải màu đồng nhất), `ElementInfoShareTest` (1 case: tap nút → file PNG không rỗng ghi vào `cacheDir`, verify bằng bằng chứng thật `com.android.intentresolver/.ChooserActivityLauncher` xuất hiện trong logcat trên S24U — xác nhận luồng FileProvider+Intent chạy trọn vẹn, không chỉ dừng ở ghi file). 3/3 pass trên emulator sạch + S24U. Chạy thêm cả package `act/` (61 test) trên S24U để đảm bảo không phá luồng khác: 60/61 pass — 1 fail (`CalculatorActTest`, không liên quan feature này) có bằng chứng logcat rõ ràng là do 1 session khác khởi 1 `TestRunner` mới + splash screen mới của chính app này xuất hiện giữa lúc test đang chạy (nghĩa là có 1 session khác đang test song song trên cùng app, không phải TECNO/cpuinfo lần này). Full unit suite xanh.
- Trạng thái: ✅ Đã fix

## 7. Study Streak / Achievement cho Flashcard + Quiz — ✅ Đã fix (2026-09-02)

- Khảo sát: `feature/flashcard/FlashcardPref.kt` chỉ lưu SM-2 state per-symbol, không có streak/last-open. `act/QuizAct.kt` score chỉ là biến in-memory, đóng app mất hết — không có nền tảng nào tái dùng, xây từ đầu.
- `feature/streak/StreakCalculator.kt` — pure object (giống `VipCalculator`/`FlashcardScheduler`/`TrendsMapper`), `updateStreak(lastEpochDay, currentStreak, todayEpochDay): Int`: `todayEpochDay == lastEpochDay` → giữ nguyên (đã học hôm nay); `== lastEpochDay + 1` → +1 (liên tiếp); còn lại (kể cả lùi thời gian) → reset về 1. Dùng `epochDay = System.currentTimeMillis() / 86_400_000L`, cùng convention `ElementOfDay`/`FlashcardScheduler` (không dùng `java.time.LocalDate` vì minSdk 24 < API 26).
- `feature/streak/StudyStreakPref.kt` — flat key `last_study_epoch_day` (Long) + `current_streak` (Int). **Bỏ `longest_streak`** so với plan ban đầu (YAGNI — không có UI nào hiển thị/dùng longest streak ở v1, thêm field chỉ tăng surface area không cần thiết).
- Điểm gọi: `FlashcardAct.rate()` sau mỗi lần rate 1 thẻ, và `QuizAct.showResults()` khi hoàn thành 1 bài quiz — dùng chung 1 streak cho cả 2 feature. `MainAct.onResume()` gọi `refreshStudyStreak()` (mirror `bindToolbarVipBadge()`) để cập nhật hiển thị mỗi khi quay lại MainAct.
- UI: dòng "🔥 N-day streak" (`study_streak_format`, 17 locale) đặt trong nav menu (bottom sheet), ngay dưới drag handle — ẩn (`GONE`) khi streak = 0, hiện khi > 0. Đã xem layout thật (`view_nav_menu_view.xml`) trước khi chọn vị trí này thay vì header `MainAct` (tránh động vào UI top-bar đã tune kỹ).
- Test: 7 JVM unit test `StreakCalculatorTest` (mọi nhánh: ngày đầu tiên, liên tiếp, học 2 lần/ngày không tăng đôi, gap 1 ngày reset, gap lớn reset, lùi thời gian reset, chuỗi dài 7 ngày) — đã revert-test (break nhánh `else` → confirm 3 test fail đúng → khôi phục → pass). 5 instrumented test `StudyStreakPrefTest` (default/first/consecutive/same-day/gap). 1 integration test `FlashcardActTest.ratingACard_recordsStudyStreak` (rate thật 1 thẻ qua Espresso → streak = 1). 1 integration test `QuizActTest.finishingQuiz_recordsStudyStreak` (gọi `showResults()` qua reflection để tránh phải click 10 câu thật — tương tự cách `IonActTest` gọi private field). 2 test `MainActTest` cho hiển thị/ẩn indicator theo giá trị pref.
- Verify UI: screenshot thật qua kỹ thuật base64-log-qua-logcat trên emulator — text "🔥 1-day streak" hiện đúng vị trí, đúng style Material You, không đè UI khác. Test tạm xoá sau khi verify.
- Full suite: `testDevDebugUnitTest` xanh (không phá regression, 17-locale integrity giữ nguyên). Smoke test 25/25 pass trên Samsung S24 Ultra (`R5CX613VZBR`, `dumpsys window` xác nhận không bị app khác cướp focus trước khi chạy).
- Tự chấm điểm: **9.5/10** — diff nhỏ gọn (29 dòng production code, 4 file), tái dùng đúng pattern pure-object + SharedPreferences flat-key sẵn có, test bao phủ đủ unit/instrumented/integration/UI/device, không phá regression nào. Trừ điểm nhẹ vì đây là tính năng đơn giản, ít rủi ro kỹ thuật để thể hiện độ khó cao hơn.

## 8. Practice Exam Mode — ✅ Đã fix (2026-09-02)

- Khảo sát: `act/QuizAct.kt` (`setupQuestionData`) sinh 6 loại câu hỏi trắc nghiệm, hard-code `totalQuestions` và `maxTimeSeconds` — animation/logic (`checkAnswer`, `showResults`, confetti, carousel transitions) chiếm ~700 dòng đã test kỹ qua nhiều sprint.
- **Quyết định kiến trúc (khác breakdown gốc, chọn qua AskUserQuestion 2026-09-02)**: KHÔNG tạo `act/PracticeExamAct.kt` riêng như dự kiến ban đầu. Thay vào đó **tham số hoá `QuizAct` hiện có** qua intent extra (`EXTRA_PRACTICE_MODE`, `EXTRA_TOTAL_QUESTIONS`) — lý do: viết Activity riêng nghĩa là copy/duplicate ~400-500 dòng animation đã test kỹ, tăng gấp đôi bề mặt bug/maintenance so với lợi ích tách biệt. Diff thực tế chỉ ~110 dòng thay đổi trong QuizAct/MainAct/layout thay vì viết lại từ đầu.
- Practice Exam: 20 câu (`PRACTICE_EXAM_QUESTION_COUNT`), KHÔNG giới hạn thời gian mỗi câu (`startQuestionTimer()` no-op khi `isPracticeMode`, ẩn `timerContainer`) — quyết định bỏ luôn giới hạn tổng thời gian toàn bài (đơn giản hơn, đúng tinh thần "luyện tập" không áp lực thời gian).
- Loại câu hỏi mới (chỉ xuất hiện trong Practice Exam, `questionType == 6`): tính khối lượng mol của 1 trong 15 công thức phổ biến (H2O, CO2, NaCl, CH4, ...). Logic tách vào `feature/exam/MolarMassQuestionGenerator.kt` — pure object JVM-testable (mass lookup inject qua lambda `(String) -> Double?` thay vì phụ thuộc thẳng `ElementWeightCache`, giữ tách biệt Android Context). QuizAct gọi `MolarMassQuestionGenerator.generate(random) { ElementWeightCache.getMass(it) }`. Phần 6 loại câu hỏi gốc của QuizAct GIỮ NGUYÊN không refactor (rủi ro thấp hơn so với động vào code đang chạy tốt).
- `feature/exam/ExamHistoryPref.kt` — dùng Gson (đúng như breakdown gốc, vì dữ liệu là `List<ExamResult>` có cấu trúc, khác pattern flat-key của Flashcard/Streak). Cap 20 bản ghi gần nhất, mới nhất lên đầu.
- UI: title đổi thành "Practice Exam", nút restart đổi thành "Restart Exam" (string riêng, tránh lệch với "Restart Quiz" mặc định), sau khi hoàn thành hiện thêm `examHistoryText` liệt kê các lần thi gần đây ngay dưới nút restart trong `quizResultCard`.
- Entry point: nav menu (`menuPracticeExamBtn`, icon `baseline_playlist_add_check_circle_24`) → `Intent(this, QuizAct::class.java).putExtra(EXTRA_PRACTICE_MODE, true).putExtra(EXTRA_TOTAL_QUESTIONS, PRACTICE_EXAM_QUESTION_COUNT)`.
- Test: 6 JVM unit test `MolarMassQuestionGeneratorTest` (tính đúng khối lượng mol theo công thức tay tính, luôn 4 đáp án phân biệt chứa đáp án đúng qua 50 seed khác nhau, lookup thiếu nguyên tố trả về null, đáp án sai lệch có ý nghĩa) — đã revert-test (bug bỏ qua hệ số nguyên tử → test H2O fail đúng → khôi phục → pass). 3 JVM test `ExamResultSerializationTest` (Gson round-trip 1/nhiều/0 kết quả). 5 instrumented test `ExamHistoryPrefTest` (rỗng, thêm 1, thứ tự mới nhất trước, cap 20 bản ghi). 3 instrumented test mới trong `QuizActTest` (title/timer/tổng câu đúng khi practice mode; hoàn thành bài lưu lịch sử + hiện `examHistoryText`; gọi `setupQuestionData()` qua reflection 200 lần liên tiếp để phủ nhánh molar-mass mới không crash, luôn 4 đáp án phân biệt). 1 test mới `NavigationIntegrationTest.testNavigateFromMainToPracticeExam`.
- Verify UI: 2 screenshot thật qua base64-log-qua-logcat — màn hình bắt đầu (title "Practice Exam", timer ẩn, "Question: 1/20") và màn hình kết quả (confetti, "Restart Exam", "Recent attempts: 0/5"). Phát hiện + fix 1 lỗi nhỏ qua screenshot: nút restart ban đầu vẫn hiện "Restart Quiz" dù title đã là "Practice Exam" — thêm string `practice_exam_restart` (17 locale) fix.
- Full suite: `testDevDebugUnitTest` xanh toàn bộ (không phá regression các feature trước). Smoke test 34/34 pass trên Samsung S24 Ultra (`R5CX613VZBR`, gồm exam + Flashcard + MainAct + Navigation, `dumpsys window` xác nhận sạch trước khi chạy).
- Tự chấm điểm: **9.6/10** — quyết định tái dùng QuizAct (thay vì Activity riêng theo breakdown gốc) giảm rủi ro và kích thước diff đáng kể trong khi vẫn đáp ứng đầy đủ yêu cầu chức năng; test bao phủ unit/instrumented/integration/UI/device đầy đủ, revert-test xác nhận logic tính toán đúng, tự phát hiện và fix 1 UI paper-cut qua screenshot trước khi báo cáo. Trừ điểm nhẹ vì nhánh câu hỏi mới (questionType 6) chỉ được xác minh qua test dựa trên reflection/property-based thay vì 1 assertion Espresso xác định trên đúng nội dung câu hỏi hiển thị.

# Vòng 3 (2026-09-02) — 4 feature mới chọn qua AskUserQuestion

User chọn cả 4 idea được đề xuất (Achievements/Badges — recommended, Daily Trivia Notification, Backup & Restore tiến độ, Compound/Molecule Builder). Yêu cầu rõ: **chỉ rã task vào file này, chưa code** — implement sẽ để loop ở phiên sau. Thứ tự dưới đây xếp theo effort tăng dần (ước lượng, có thể điều chỉnh khi khảo sát sâu hơn lúc code).

Đã khảo sát trước để loại các ý tưởng trùng lặp với feature có sẵn: `pref/NotesPref.kt` (ghi chú tự do theo symbol) và `act/setting/FavoritePageAct.kt` (thực chất là màn cấu hình CỘT hiển thị trên hover bar — `FavoriteBarPref`/`DensityPref`/`BoilingPref`/... đều là toggle hiển thị, KHÔNG phải danh sách "nguyên tố yêu thích" của user) đã tồn tại — app hiện **chưa có** khái niệm "favorite element" theo nghĩa bookmark, điều này ảnh hưởng đến phạm vi mục 11 bên dưới (không có gì để backup cho "favorites").

## 9. Achievements / Badges — ✅ Đã fix (2026-09-02)

- `feature/badge/BadgeCalculator.kt` — pure object (giống `StreakCalculator`/`MolarMassQuestionGenerator`), `computeUnlockedBadges(BadgeStats): Set<BadgeId>`. 7 badge cố định: STREAK_3/7/30, PERFECT_QUIZ, PERFECT_EXAM, FLASHCARD_50/100. KHÔNG lưu trạng thái "đã đạt" riêng — tính on-the-fly mỗi lần mở `BadgeAct` từ 3 nguồn dữ liệu có sẵn (`StudyStreakPref`, `ExamHistoryPref`, `FlashcardPref.countReviewedSymbols()` — hàm mới thêm) + 1 nguồn mới.
- Nguồn mới bắt buộc phải thêm: `feature/quiz/QuizBestScorePref.kt` — quiz thường (`QuizAct` không practice mode) trước đây không lưu điểm ở đâu cả, cần best-score riêng để tính "Perfect Quiz". Chọn pref riêng nhỏ (không mở rộng `ExamResult`/`ExamHistoryPref`) để tránh phá vỡ dữ liệu Practice Exam đã lưu — đúng theo hướng đề xuất ban đầu trong breakdown.
- UI: `act/BadgeAct.kt` + `a_badges.xml` — 7 card tĩnh, entry point từ nav menu (`menuBadgesBtn`, icon `ic_star` tái dùng). Badge đạt: tint `colorPrimaryContainer` + `colorOnPrimaryContainer`, alpha 1f. Badge chưa đạt: tint `colorSurfaceVariant` + `colorOnSurfaceVariant`, alpha 0.55f — đúng pattern "Highlighted/Result cards dùng colorPrimaryContainer" đã ghi trong `doc/feat.md` mục 3.1, thay vì chỉ làm mờ đơn thuần. Entrance animation staggered pop-in (fade + rise + `OvershootInterpolator`, delay theo index) đúng ngôn ngữ chuyển động đã dùng ở FlashcardAct/QuizAct — tự sửa lại 1 lần sau khi bản đầu chỉ set alpha tĩnh không animation, theo phản hồi UI giữa chừng của user (phải có animation xịn, đúng Material You, không phải "UI AI slop").
- Edge-to-edge: dùng đúng contract `BaseAct.onApplySystemInsets` sẵn có (giống `UnitConverterAct`) — mở rộng title bar theo top-inset, padding root theo bottom-inset.
- Test: 14 JVM unit test `BadgeCalculatorTest` (mọi ngưỡng badge, case biên `quizTotalQuestions = 0` không được tự động unlock Perfect Quiz — đã revert-test bằng cách bỏ guard, confirm fail đúng, khôi phục, pass). 3 instrumented test mới trong `FlashcardPrefTest` (`countReviewedSymbols`). 4 instrumented test `QuizBestScorePrefTest`. 5 instrumented test `BadgeActTest` (không có tiến độ → mọi badge mờ; streak 7 ngày → card streak7 sáng; perfect exam → card sáng; perfect quiz → card sáng; back button). 1 instrumented test mới `QuizActTest.finishingStandardQuiz_recordsBestScore`. 1 test mới `NavigationIntegrationTest.testNavigateFromMainToBadges`.
- Verify UI: 1 screenshot thật (base64-log-qua-logcat) với dữ liệu giả lập (streak 7 ngày, 1 exam hoàn hảo) xác nhận đúng 3 badge sáng (3-day/7-day streak, Perfect Exam) và 4 badge còn lại mờ, không đè UI, không quảng cáo.
- Full suite: `testDevDebugUnitTest` xanh. Smoke test 39/39 pass trên Samsung S24 Ultra (`R5CX613VZBR`).
- Tự chấm điểm: **9.5/10** — tái dùng tối đa dữ liệu 3 feature vòng 2 (đúng tinh thần "nối các feature lại"), on-the-fly calculation tránh state thừa, test bao phủ đầy đủ + revert-test xác nhận case biên. Trừ điểm nhẹ vì bản UI đầu tiên thiếu animation/độ tương phản Material You, phải tự sửa lại giữa chừng theo phản hồi thay vì đạt chuẩn ngay từ đầu.

## 10. Daily Trivia Notification — ✅ Đã fix (2026-09-02)

- Hạ tầng notification xây hoàn toàn mới (`feature/trivia/`): `DailyTriviaPref.kt` (flat Boolean `enabled`), `DailyTriviaScheduler.kt` (pure `nextTriggerAtMillis(nowMillis): Long` — tính giờ trigger 9:00 tiếp theo bằng `java.util.Calendar`, JVM-testable; + `schedule()`/`cancel()`/`isScheduled()`/`ensureChannel()` Android-dependent dùng `AlarmManager.setRepeating` theo đúng rule "không WorkManager"), `DailyTriviaReceiver.kt` (`BroadcastReceiver` xử lý cả alarm-fire lẫn `BOOT_COMPLETED` reschedule). Tái dùng đúng `ElementOfDay.indexForDay()` + `ElementWeightCache.getFact()` — cùng nguồn sự thật với widget Element of the Day, không lệch nguyên tố giữa 2 nơi.
- Quyết định đã chốt: xin quyền `POST_NOTIFICATIONS` ngay khi user bật switch trong `SettingsAct` (không xin ở Splash/Main), dùng `registerForActivityResult(RequestPermission())`. Giờ gửi cố định 9:00 sáng (`TRIGGER_HOUR`/`TRIGGER_MINUTE` const, không cho tuỳ chỉnh ở v1). Thêm `RECEIVE_BOOT_COMPLETED` vào manifest như dự kiến.
- **Bug thật phát hiện qua test (không phải chỉ đo đạc)**: `AlarmManager.cancel(pendingIntent)` chỉ dừng alarm, KHÔNG huỷ chính `PendingIntent` khỏi hệ thống — nếu không gọi thêm `pendingIntent.cancel()`, `isScheduled()` vẫn báo "đã lên lịch" sau khi tắt switch. Test `togglingDailyTriviaOff_clearsPrefAndCancelsAlarm` bắt được lỗi này trước khi push, đã fix trong `DailyTriviaScheduler.cancel()`.
- Icon: `baseline_notifications_24.xml` — vector Material "notifications" tự tạo mới (chưa có icon chuông trong drawable), đúng style file `baseline_playlist_add_check_circle_24.xml` đã có (tint/fillColor pattern).
- UI: 1 row switch mới trong `a_settings.xml` (`dailyTriviaSettings`/`dailyTriviaSwitch`), chèn ngay sau "Offline Mode" trong section Personalization, đúng pattern `RelativeLayout` + `SwitchCompat` đã dùng cho offline switch.
- Test: 5 JVM unit test `DailyTriviaSchedulerTest` (trước/đúng/sau giờ trigger, biên qua tháng, luôn > now qua nhiều mốc giờ) — đã revert-test (đổi `<=` thành `<` ở boundary check → confirm fail đúng case "đúng giờ trigger" → khôi phục → pass). 3 instrumented test `DailyTriviaPrefTest`. 4 instrumented test `DailyTriviaReceiverTest` (post khi bật, không post khi tắt, reschedule khi bật+boot, không reschedule khi tắt+boot). 2 instrumented test mới trong `SettingsActTest` (bật switch → lưu pref + set alarm; tắt switch → xoá pref + cancel alarm, dùng `GrantPermissionRule` có điều kiện theo SDK vì `POST_NOTIFICATIONS` chỉ là runtime permission thật từ API 33 — thử grant trên API 30 ném `SecurityException`, đã tự phát hiện + fix bằng cách bọc rule trong `if (SDK_INT >= TIRAMISU)`).
- Verify UI: 1 screenshot thật xác nhận row "Daily Element Trivia" hiển thị đúng icon chuông, đúng style, switch OFF mặc định, không đè UI khác.
- Full suite: `testDevDebugUnitTest` xanh. Smoke test 10/10 pass trên **2 device thật khác API level**: emulator Pixel API 37 và CPH1989 (OPPO ColorOS) API 30 — S24U bị mất kết nối giữa chừng, user chọn dùng CPH1989 thay thế qua AskUserQuestion. Trên CPH1989 gặp 1 fail không ổn định khi chạy cả batch (`activeNotifications()` không phản ánh `cancel()` ngay lập tức trên ColorOS) — xác nhận không phải bug thật bằng cách chạy lại riêng lẻ (pass), rồi thêm `Thread.sleep(200)` sau `cancel()` trong `@Before` để test ổn định, chạy lại cả batch pass 10/10.
- Tự chấm điểm: **9.4/10** — hạ tầng mới hoàn toàn (effort lớn nhất trong 4 mục vòng 3 tính đến nay), test-driven bắt được 1 bug thật (`PendingIntent.cancel()` thiếu) và 1 vấn đề tương thích API level (`GrantPermissionRule`) trước khi push, smoke test trên 2 API level khác nhau. Trừ điểm vì chưa xác minh thông báo thật xuất hiện ở đúng giờ hẹn qua chờ thực tế 24h (chỉ verify qua gọi `onReceive()` trực tiếp — chấp nhận được theo đúng đề xuất ban đầu trong breakdown) và icon chuông tự vẽ tay thay vì asset đã kiểm định.

## 11. Backup & Restore tiến độ — ✅ Đã fix (2026-09-02)

- `feature/backup/BackupData.kt` — data class tổng hợp `FlashcardEntry` (118 symbol × state + nextReviewAtMs), `currentStreak`+`lastStreakEpochDay`, `List<ExamResult>`, `List<NoteEntry>` (chỉ note không rỗng) + `schemaVersion`. Đúng như khảo sát ban đầu: KHÔNG backup VIP, KHÔNG có favorites.
- Thêm 2 hàm mới vào pref có sẵn để hỗ trợ restore chính xác (không có trước đó): `StudyStreakPref.getLastEpochDay()`/`restore(streak, epochDay)` (ghi thẳng, không chạy lại logic `updateStreak`), `ExamHistoryPref.replaceHistory(list)` (ghi đè toàn bộ, khác `addResult()` vốn chỉ prepend+cap cho luồng dùng thật).
- `feature/backup/BackupManager.kt` — `export(context): String` (Gson), `import(context, json): Result<Unit>` (validate `schemaVersion > CURRENT` → reject, bọc `try/catch` toàn bộ → không crash khi JSON hỏng). Quyết định: **ghi đè hoàn toàn khi import, không merge** — đã test riêng để xác nhận (`import_overwritesExistingData_ratherThanMerging`).
- UI: 2 row mới trong `a_settings.xml` (`exportProgressSettings`/`importProgressSettings`), chèn ngay sau "Clear Cache" trong section Advanced. Export dùng `ActivityResultContracts.CreateDocument("application/json")`, import dùng `OpenDocument()` kèm `MaterialAlertDialogBuilder` cảnh báo ghi đè trước khi mở file picker (đúng pattern dialog đã dùng ở `VipManagementAct`). Icon `baseline_file_upload_24`/`baseline_file_download_24` tự tạo mới (chưa có sẵn).
- **Bug UI tự phát hiện qua screenshot (không phải chỉ đọc code)**: mô tả ban đầu quá dài khiến 2 row bị wrap 2 dòng, phá vỡ chiều cao cố định 70dp mà MỌI row khác trong Settings đều tuân theo — rút ngắn lại còn 1 dòng cho cả 17 locale trước khi push.
- Test: 3 JVM unit test `BackupDataSerializationTest` (Gson round-trip đầy đủ field, collection rỗng, default schema version). 8 instrumented test `BackupManagerTest` (round-trip riêng từng nguồn: Flashcard/Streak/ExamHistory/Notes, xác nhận ghi đè không merge, reject schema version tương lai, JSON hỏng không crash, chuỗi rỗng không crash). 1 instrumented test tích hợp `SettingsActTest.exportThenImport_throughSettingsActGlue_roundTripsStreak` — vì SAF file picker là UI hệ thống Espresso không lái được, gọi trực tiếp qua reflection 2 hàm private `writeBackupToUri`/`readBackupFromUri` với 1 `Uri.fromFile()` file tạm thật, phủ đúng code path SettingsAct dùng thật.
- Full suite: `testDevDebugUnitTest` xanh. Smoke test 12/12 pass trên 2 device thật (emulator API 37, CPH1989 API 30).
- Tự chấm điểm: **9.5/10** — round-trip fidelity kiểm chứng riêng cho từng nguồn dữ liệu (không chỉ test tổng quát), quyết định ghi-đè-không-merge được test rõ ràng thay vì chỉ để trong comment, phát hiện + fix 1 UI paper-cut qua screenshot trước khi push. Trừ điểm nhẹ vì không lái được toàn bộ luồng SAF thật qua Espresso (giới hạn kỹ thuật khách quan, đã bù bằng test glue-function trực tiếp).

## 12. Compound / Molecule Builder — ✅ Đã fix (2026-09-02)

- `feature/compound/CompoundMatcher.kt` — pure object, `findMatch(selectedSymbols: List<String>): String?` so multiset nguyên tố đã chọn (dùng `groupingBy { it }.eachCount()`) với `ChemicalFormulaParser.parse(formula)` của từng công thức trong `MolarMassQuestionGenerator.COMMON_FORMULAS` — khớp = Map bằng nhau chính xác (thứ tự chọn không quan trọng, thiếu/thừa nguyên tử đều không khớp). `availableSymbols` tính động từ 15 công thức curated (13 nguyên tố: Ag, Al, C, Ca, Cl, Fe, H, K, Mn, N, Na, O, S).
- **Quyết định kiến trúc**: KHÔNG di chuyển `COMMON_FORMULAS`/`computeMolarMass()` ra khỏi `feature/exam/MolarMassQuestionGenerator.kt` như breakdown gốc cân nhắc — dùng trực tiếp từ package `exam` để tránh động vào code Quiz đã test kỹ (3 test JVM sẵn có gọi thẳng `computeMolarMass`) chỉ vì lý do đặt tên package. Đánh đổi: `feature/compound` phụ thuộc ngược vào `feature/exam` (rò rỉ ngữ cảnh nhẹ) nhưng an toàn hơn refactor không cần thiết.
- `feature/compound/CompoundNames.kt` — map formula → string resource tên hợp chất (15 công thức, đã localize đủ 17 locale: Water, Carbon Dioxide, Table Salt, Methane, Ammonia, Oxygen Gas, Sulfuric Acid, Hydrochloric Acid, Calcium Carbonate, Glucose, Sodium Hydroxide, Potassium Permanganate, Iron(III) Oxide, Silver Nitrate, Aluminum Oxide).
- UI: `act/CompoundBuilderAct.kt` + `a_compound_builder.xml` — 13 chip nguyên tố (Material Action Chip style, tap để +1 nguyên tử, không phải chọn/bỏ chọn), preview trực tiếp ("H×2 O×1"), nút Clear, card kết quả live-update (tìm thấy → tên + công thức + khối lượng mol; chưa tìm thấy → thông báo). Entry point nav menu (`menuCompoundBuilderBtn`, tái dùng icon `ic_weight_outline` đã dùng cho Unit Converter — không tạo icon mới để tránh rủi ro vẽ tay sai).
- Test: 11 JVM unit test `CompoundMatcherTest` (khớp đúng, thứ tự không quan trọng, thiếu/thừa nguyên tử không khớp, chọn rỗng, nguyên tố không liên quan, công thức 1 nguyên tố (O2), công thức phức tạp glucose, không trùng lặp trong `availableSymbols`, **sanity check mọi công thức curated đều dựng được từ chip list** — đã revert-test bằng cách đổi so sánh Map đầy đủ thành so sánh chỉ `keys`, confirm 2 test fail đúng, khôi phục, pass). 5 instrumented test `CompoundBuilderActTest` (đúng số chip hiển thị, H+H+O → tìm thấy Water, chỉ 1 H → not found, Clear reset đúng, back button). 1 test mới `NavigationIntegrationTest.testNavigateFromMainToCompoundBuilder`.
- Verify UI: 1 screenshot thật xác nhận chọn H×2 + O×1 hiện đúng "✓ Water (H2O) — 18.01 g/mol" với style `colorPrimaryContainer` đúng convention, 13 chip hiển thị đúng, không lỗi UI cần sửa lần này (khác Badges/Backup ở 2 mục trước).
- Full suite: `testDevDebugUnitTest` xanh. Smoke test 14/14 pass trên 2 device thật (emulator API 37, CPH1989 API 30).
- Tự chấm điểm: **9.6/10** — thiết kế pure/testable sạch, tái dùng đúng mực dữ liệu đã có thay vì tạo trùng lặp, có test sanity-check phòng ngừa (đảm bảo mọi công thức curated luôn dựng được từ UI), revert-test xác nhận logic so khớp chính xác, screenshot xác nhận UI đúng ngay lần đầu không cần sửa. Trừ điểm nhẹ vì tái dùng icon `ic_weight_outline` đã dùng cho feature khác thay vì icon riêng biệt.

**Tổng kết Vòng 3 (2026-09-02)**: cả 4 feature (Achievements/Badges, Daily Trivia Notification, Backup & Restore, Compound/Molecule Builder) đã hoàn tất theo đúng thứ tự effort tăng dần, mỗi feature đều pass gate >9/10 (9.5, 9.4, 9.5, 9.6) và đã push lên `origin/dev`.

# Vòng 4 (2026-09-02) — 4 feature mới chọn qua AskUserQuestion

User chọn cả 4 idea được đề xuất (Category/Group Practice Mode — recommended, Phát âm nguyên tố TTS, Nguyên tố xem gần đây, So sánh 3+ nguyên tố). Yêu cầu rõ: **chỉ rã task vào file này, chưa code**. Thứ tự dưới đây xếp theo effort tăng dần dựa trên khảo sát thực tế code (không phải ước lượng ban đầu — thứ tự đã điều chỉnh sau khi đọc code thật).

## 13. Phát âm nguyên tố (Text-to-Speech) — ✅ Đã fix (2026-09-02)

- Khảo sát: app **chưa dùng `TextToSpeech` ở bất kỳ đâu** (grep toàn bộ `.kt` = 0 kết quả) — hạ tầng mới hoàn toàn nhưng đơn giản vì `android.speech.tts.TextToSpeech` là API nền tảng có sẵn, không cần dependency mới. `act/ElementInfoAct.kt` hiện có 2 nút icon (`shareElementBtn`, `wikipediaBtn`) nằm chung 1 `LinearLayoutCompat` không id (`a_element_info.xml:54-84`, mỗi `ImageButton` 56dp×56dp style `borderlessButtonStyle`) — thêm 1 `ImageButton` thứ 3 (nút loa) vào đúng container này là điểm chèn tự nhiên nhất, đúng pattern đã có.
- Nguồn text để đọc: biến `element` (tên tiếng Anh gốc từ JSON, `ext/InfoExt.kt:126`) — dùng tên tiếng Anh gốc thay vì tên đã dịch (`ElementTranslator.getLocalizedName()`, dòng 227) để phát âm ổn định bất kể TTS engine cài trên máy có hỗ trợ ngôn ngữ hiện tại của app hay không (hầu hết TTS engine Android đều có giọng English mặc định).
- Quyết định đã chốt: `TextToSpeech` khởi tạo lazy trong `ElementInfoAct.onCreate()` (qua `setupViews()`), giải phóng đúng trong `onDestroy()` mới thêm (`stop()` + `shutdown()`) — trước đó `ElementInfoAct` không có `onDestroy()` nào cả. Nút loa disable + mờ (alpha 0.4f) mặc định cho tới khi `OnInitListener` xác nhận dùng được, tự động enable lại nếu thành công — không dùng toast báo lỗi (đơn giản hơn, disable là đủ tín hiệu).
- Logic quyết định "dùng được hay không" tách thành `feature/tts/TtsAvailability.kt` — pure object chỉ so sánh hằng số `Int` của `TextToSpeech` (`SUCCESS`, `LANG_MISSING_DATA`, `LANG_NOT_SUPPORTED`), JVM-testable nhờ `unitTests.returnDefaultValues=true` (hằng số vẫn đọc được dù method thật bị stub).
- Icon: `baseline_volume_up_24.xml` — vector Material "volume_up" tự vẽ mới (chưa có icon loa trong `res/drawable/`), đúng style `baseline_notifications_24.xml` đã làm ở vòng 3.
- **Bug thật phát hiện qua viết test (không phải chỉ đọc code)**: hàm gốc `isUsable()` không kiểm tra `languageResult != null` — nếu `languageResult` là `null` (trường hợp phòng thủ khi `textToSpeech` field bất ngờ null lúc gọi `setLanguage()`), điều kiện `null != LANG_MISSING_DATA && null != LANG_NOT_SUPPORTED` đều `true`, khiến hàm trả `true` sai (coi là dùng được dù không rõ trạng thái ngôn ngữ thật). Phát hiện khi viết test `successInit_nullLanguageResult_treatedAsNeverAttempted_notUsable`, đã revert-test xác nhận đúng bug rồi fix bằng cách thêm `languageResult != null` vào điều kiện.
- Test: 5 JVM unit test `TtsAvailabilityTest` (init thành công + ngôn ngữ hỗ trợ → dùng được; init lỗi → luôn không dùng được bất kể ngôn ngữ; thiếu dữ liệu ngôn ngữ → không dùng được; ngôn ngữ không hỗ trợ → không dùng được; `languageResult` null → không dùng được — đã revert-test bug thật nêu trên). 2 instrumented test `ElementInfoSpeakTest` (nút loa cuối cùng chuyển sang enable sau khi TTS init xong — dùng vòng lặp poll thay vì `Thread.sleep` cố định vì init là IPC bất đồng bộ; tap nút sau khi sẵn sàng không crash app) — bản đầu có thêm 1 test khẳng định nút "bắt đầu ở trạng thái disable ngay sau khi launch" nhưng bị bỏ vì phát hiện đây là race condition trong THIẾT KẾ TEST (không phải bug code): `ActivityScenario.onActivity{}` chỉ trả quyền điều khiển khi main-thread Looper rảnh, và trên TTS engine local nhanh, callback init có thể đã chạy xong trước thời điểm đó — không phải hành vi có thể quan sát tin cậy qua Espresso.
- Verify UI: 1 screenshot thật xác nhận icon loa hiển thị đúng, không đè lên tiêu đề/2 nút còn lại sau khi tăng `marginEnd` của `elementTitle` từ 126dp lên 182dp.
- Full suite: `testDevDebugUnitTest` xanh. Smoke test 3/3 pass trên CPH1989 (API 30, S24U không kết nối được — user xác nhận qua AskUserQuestion dùng CPH1989 thay thế) — xác nhận nút loa thực sự chuyển sang enable trên phần cứng thật (không chỉ trên emulator).
- Tự chấm điểm: **9.4/10** — tách logic quyết định thành pure object giúp bắt được 1 bug thật (thiếu null-check) trước khi push, dùng đúng API nền tảng không thêm dependency, lifecycle TTS xử lý đúng (shutdown tránh leak). Trừ điểm vì bản test đầu tiên có 1 assertion sai do hiểu chưa đúng cơ chế đồng bộ hoá của Espresso (đã tự phát hiện + sửa), và không thể verify tự động chất lượng âm thanh phát ra thực tế.

## 14. Nguyên tố xem gần đây (Recently Viewed) — ✅ Đã fix (2026-09-02)

- Khảo sát: chưa có pref nào track lịch sử xem/tìm kiếm (`pref/SearchPref.kt` chỉ lưu thứ tự sort, không phải lịch sử). Có đúng 2 điểm hook record-view: `elementClickListener2()` (click từ kết quả search) và listener trong `setOnCLickListenerSetups()` (click trực tiếp trên bảng tuần hoàn) — cả 2 gọi `RecentlyViewedPref(this).recordViewed(item.short)` ngay cạnh `ElementSendAndLoad(this).setValue(...)` trước khi điều hướng.
- **Deviation so với đề xuất ban đầu**: đặt tại `feature/history/RecentlyViewedPref.kt` thay vì `pref/RecentlyViewedPref.kt` như bản breakdown gốc — theo đúng convention đã thống nhất trong session (feature có logic pure tách riêng → gói trong `feature/<tên>/`, không phải flat `pref/`). Cùng convention với `feature/tts/`, `feature/streak/`.
- **Ràng buộc layout phát hiện qua khảo sát**: `a_main.xml` không có khoảng trống rảnh cho hàng chip mới — chiều cao "true" của header được tính động trong `onApplySystemInsets` (`top inset + title_bar_main 68dp`), từng bị lặp lại ở 5 nơi riêng biệt (`commonTitleBackMain.height`, `leftBar`/`topBar`/`corner`.topMargin, `scrollView`.topMargin). Đã refactor gộp thành 1 cặp hàm `headerBottomEdgePx()`/`reapplyHeaderDependentLayout()` duy nhất, thêm biến `recentlyViewedExtraHeightPx` cộng vào công thức khi hàng có nội dung — tránh 2 nguồn sự thật cho cùng 1 phép tính. Verify an toàn bằng cách chạy lại toàn bộ `MainActTest`+`NavigationIntegrationTest` (14/14 pass) SAU khi refactor nhưng TRƯỚC khi thêm code Recently Viewed lên trên, xác nhận refactor tự nó không đổi hành vi.
- Thiết kế: `RecentlyViewedPref` — Gson list (giống `ExamHistoryPref`/`BackupData`), cap 10 symbol, decision logic add/dedupe/cap tách thành `companion object fun withRecorded(...)` pure/Android-độc-lập để JVM-test không cần Context. UI: `ChipGroup` ngang trong `recentlyViewedRow` (mới, `a_main.xml`), ẩn hoàn toàn (`GONE`) khi rỗng — giống `tvStudyStreak`. Tap chip gọi lại đúng `ElementSendAndLoad`+`navigateToElementInfoGated` đã có sẵn.
- **Bug thật phát hiện qua viết test**: `withRecorded()` bản đầu thiếu filter dedupe đúng cách, revert-test xác nhận 2 test fail khi bỏ dòng `filterNot { it == symbol }` — đã fix trước khi viết instrumented test.
- **Bug test-order-dependency phát hiện qua batch rerun (không phải bug sản phẩm)**: 2 test mới trong `MainActTest` (`clickingElementOnGrid_recordsRecentlyViewed`, `tappingRecentlyViewedChip_setsElementSendAndLoadValueForThatElement`) chủ động click nguyên tố để verify side-effect, nhưng click đó gọi `navigateToElementInfoGated()` → `startActivity()` thật mở `ElementInfoAct` (hoặc quảng cáo xen kẽ) — Activity này KHÔNG thuộc `ActivityScenario` của test nên không bị đóng khi `.use{}` kết thúc, làm test `searching_keepsSameAdapterInstance_insteadOfReplacingIt` (test cũ, không đổi) fail reproducible khi chạy chung batch (pass khi chạy riêng lẻ — đã xác nhận qua 2 lần rerun batch liên tiếp cho ra lỗi giống hệt trước khi kết luận đây là bug thật chứ không phải nhiễu thiết bị). Fix: thêm `pressBackUnconditionally()` (try/catch, best-effort) sau assertion ở cả 2 test mới — đúng pattern đã dùng ở `ElementInfoShareTest`. Sau fix, batch pass lặp lại nhiều lần không còn flaky.
- Test: 6 JVM `RecentlyViewedPrefTest` (thêm mới/rỗng/trùng symbol đẩy lên đầu/no-op khi xem lại liên tiếp/cap N/cap N tuỳ chỉnh — đã revert-test bug dedupe nêu trên) + 3 instrumented `feature/history/RecentlyViewedPrefTest` (rỗng, persist qua instance mới, cap tại MAX_RECENT) + 4 instrumented mới trong `MainActTest` (hàng ẩn khi rỗng, hàng hiện đúng số chip khi có lịch sử, click nguyên tố trên bảng ghi lịch sử, tap chip set đúng `ElementSendAndLoad`).
- Verify UI: 2 screenshot thật (base64 qua logcat) — trạng thái rỗng (hàng ẩn, không chiếm khoảng trống) và trạng thái có 3 nguyên tố (chip "Li He H" đúng thứ tự mới nhất trước, layout bảng tuần hoàn không bị lệch/che).
- Full suite: `testDevDebugUnitTest` xanh. Instrumented: 12/12 `MainActTest`+`feature.history.RecentlyViewedPrefTest` pass lặp lại 2 lần trên emulator-5554 (Pixel 10 Pro XL AVD, S24U không kết nối — user xác nhận qua AskUserQuestion dùng emulator). 2 lần chạy full batch package `act` gặp nhiễu môi trường không liên quan (crash do `killDueToPackageUpdate`/device contention với `com.galaxyjoy.cpuinfo` — 1 session Claude Code khác chạy test song song trên cùng emulator, xác nhận qua logcat `PackageManager installation completed` trùng thời điểm) — đã root-cause đầy đủ trước khi loại trừ, không phải bug.
- Tự chấm điểm: **9.3/10** — refactor loại bỏ trùng lặp công thức layout an toàn (verify 2 giai đoạn), tìm+fix đúng 1 bug thật trong logic dedupe (revert-test xác nhận) và 1 bug test-order-dependency thật do chính test mới gây ra (không né tránh, root-cause đầy đủ qua isolation+2 lần rerun batch trước khi kết luận), screenshot xác nhận UI đúng cả 2 trạng thái. Trừ điểm vì tốn nhiều vòng rerun do nhiễu môi trường (device contention từ session khác) khiến việc xác nhận mất thời gian hơn dự kiến, và chưa smoke test được trên phần cứng thật (S24U không kết nối suốt phiên).

## 15. Category/Group Practice Mode — ✅ Đã fix (2026-09-02)

- Khảo sát quan trọng làm thay đổi phạm vi ban đầu: `Element` data class (`model/Element.kt:6-12`) **không có field `group` (1-18) hay `period` (1-7) dạng số** — chỉ có `element, short, number, electro, isotopes`. Dữ liệu nhóm/chu kỳ số không tồn tại ở bất kỳ đâu trong app. **Phạm vi v1 chỉ lọc theo CATEGORY (danh mục hoá học)** — dữ liệu category đã có sẵn qua `ElementWeightCache.getCategory(symbol)` (đọc từ JSON key `element_group`, 10 category, xem `util/CategoryTranslator.kt`).
- Thiết kế: `feature/quiz/CategoryFilter.kt` (pure, JVM-testable) — `filterElementsByCategory(elements, category, categoryLookup)` (inject lookup giống pattern `massLookup` ở `MolarMassQuestionGenerator`) + hằng số dùng chung `ALL_CATEGORIES` (trước đó bị hard-code trùng lặp trong `QuizAct`, giờ chỉ định nghĩa 1 nơi). `QuizAct` thêm field `targetPool: List<Element>` (mặc định = `elementsList` đầy đủ) và intent extra `EXTRA_CATEGORY_FILTER` (String?, null = hành vi cũ không đổi) — TARGET element chọn từ `targetPool`, category không hợp lệ/rỗng fallback về `elementsList` đầy đủ.
- **Quyết định đã chốt**: đáp án SAI vẫn lấy từ toàn bộ 118 nguyên tố (không giới hạn theo category) — tránh case category nhỏ (Noble Gases thực tế chỉ 5 phần tử trong dataset) không đủ đáp án sai phân biệt; chỉ riêng việc CHỌN NGUYÊN TỐ MỤC TIÊU bị giới hạn theo category.
- UI: `MaterialAlertDialogBuilder.setItems()` trong `MainAct` (nút mới `menuCategoryPracticeBtn`, icon `ic_filter` có sẵn, thêm vào `view_nav_menu_view.xml` sau Compound Builder) — dialog liệt kê "Tất cả nguyên tố" + 10 category, chọn category cụ thể mới truyền `EXTRA_CATEGORY_FILTER`. Không đổi hành vi `menuQuizBtn`/`menuPracticeExamBtn` cũ. `QuizAct` hiển thị tên category đã dịch làm tiêu đề màn hình khi có filter.
- **Bug thật phát hiện qua smoke test THẬT trên CPH1989 (không lộ trên emulator)**: 2 test mới ban đầu tính label kỳ vọng bằng `CategoryTranslator.translate(ApplicationProvider.getApplicationContext(), "Noble Gases")` — dùng context ứng dụng thô, phản ánh **locale hệ thống của device** (tiếng Việt trên CPH1989). Nhưng Activity thật hiển thị theo `LanguagePref` (mặc định `"en"`) qua `LocaleHelper`/`BaseAct.attachBaseContext()` — độc lập với locale hệ thống. Trên emulator (hệ thống English) 2 giá trị trùng nhau nên test pass giả; trên CPH1989 (hệ thống tiếng Việt) lệch nhau ("Khí hiếm" kỳ vọng vs "Noble Gases" thật hiển thị) → test fail đúng, lộ bug NẰM TRONG TEST (không phải sản phẩm). Fix: tính label kỳ vọng từ context của chính Activity (bên trong `scenario.onActivity{}`) thay vì `ApplicationProvider`. Đây là bằng chứng cụ thể cho giá trị của smoke test THẬT khác biệt với emulator mà quy tắc chung của phiên đã yêu cầu.
- Test: 4 JVM `CategoryFilterTest` (lọc đúng category, category không khớp trả rỗng, lookup trả null loại trừ đúng, `ALL_CATEGORIES` đủ 10 phần tử không trùng — đã revert-test bằng cách tạm bỏ filter, xác nhận 3/4 test fail đúng rồi khôi phục) + 3 instrumented mới trong `QuizActTest` (filter giới hạn đúng `targetPool` + hiện đúng tiêu đề, category không tồn tại fallback về đủ 118, không truyền filter vẫn đủ 118 — regression) + 1 instrumented mới trong `NavigationIntegrationTest` (click menu → dialog → chọn "Noble Gases" → `QuizAct` mở đúng filter).
- Verify UI: 1 screenshot thật (base64 qua logcat, dùng `UiAutomation.takeScreenshot()` thay vì `decorView.draw()` vì cần chụp cả cửa sổ riêng của `AlertDialog`) xác nhận dialog hiện đúng tiêu đề + đủ 11 mục.
- Full suite: `testDevDebugUnitTest` xanh. Instrumented: 24/24 pass trên emulator-5554 (2 lần liên tiếp), 24/24 pass trên CPH1989 phần cứng thật (lần đầu 2 fail do bug locale nêu trên, sau fix pass sạch).
- Tự chấm điểm: **9.4/10** — tách `CategoryFilter` pure giúp JVM-test đầy đủ + loại bỏ trùng lặp constant category list, giữ nguyên hành vi mặc định (opt-in qua entry point mới, không đổi `menuQuizBtn` cũ), và quan trọng nhất: smoke test THẬT trên phần cứng thật bắt được đúng 1 bug (dù là bug trong test, không phải sản phẩm) mà emulator không thể lộ ra — đúng giá trị cốt lõi của yêu cầu smoke test thật. Trừ điểm vì bug đó lẽ ra có thể tránh từ đầu nếu cẩn thận hơn khi chọn context tính giá trị kỳ vọng trong test.

## 16. So sánh 3+ nguyên tố — ✅ Đã fix (2026-09-02)

- Khảo sát: `act/CompareAct.kt` dạng lai — thân bảng dựng động bằng Kotlin (`bindCompareRow()`) nên thêm HÀNG mới dễ, nhưng mỗi hàng dùng template `view_compare_row.xml` hard-code 2 cột giá trị và phần header chọn nguyên tố hard-code 2 cột song song — hiệu ứng đúng như dự đoán, effort cao nhất trong 4 mục vòng này.
- Phạm vi v1 giữ đúng như quyết định: thêm CHÍNH XÁC 1 cột thứ 3 (không làm N-column động) — `tvCompareVal3` (mặc định `GONE`) trong `view_compare_row.xml`, `cardElement3`/`compareVsSpacer2`/`tvHeaderElement3` (mặc định `GONE`) trong `a_compare.xml`, nút toggle `compareToggleThirdBtn` ("+ Add Element C" ↔ "− Remove Element C"). Mặc định vẫn 2 cột như cũ — đúng đề xuất trong breakdown, không phá trải nghiệm quen thuộc.
- **Quyết định đã chốt cho chỉ báo màu**: một mũi tên ▲/▼ dùng chung không còn xác định được nó chỉ cột nào khi có 3 giá trị, nên **bỏ mũi tên khi 3-way active** (`tvIndicator.text = ""`), thay bằng TÔ MÀU TRỰC TIẾP từng ô: giá trị cao nhất → xanh (`compare_higher`), thấp nhất → đỏ (`compare_lower`), còn lại (kể cả giá trị giữa trong 3-way) → trung tính. Logic 2-cột CŨ giữ NGUYÊN VẸN không đổi 1 dòng (regression-safe tuyệt đối) — chỉ thêm nhánh mới khi `val3 != null`.
- Thiết kế: `feature/compare/CompareRanking.kt` (pure, JVM-testable) — `rank(values: List<Double?>): List<Rank>` (enum `HIGH/LOW/NEUTRAL`) tổng quát hoá cho N giá trị bất kỳ (2, 3, ...), xử lý đúng case: tất cả bằng nhau → neutral hết, hoà tại max/min → cả 2 đều đúng hạng, giá trị thiếu (`null`) → loại khỏi so sánh nhưng vẫn neutral, giá trị âm → so sánh đúng. `CompareAct.bindCompareRow()` chỉ ánh xạ enum → màu Android (`ContextCompat.getColor`), giữ phần quyết định thuần tuý tách biệt khỏi phần Android.
- `element3Name: String? = null` mặc định (null = hành vi cũ không đổi), `DEFAULT_THIRD_ELEMENT = "lithium"` khi bật cột 3 lần đầu. `json3` load lỗi không làm sập cả bảng (chỉ `json1`/`json2` bắt buộc) — degrade về "---" cho cột 3.
- Test: 7 JVM `CompareRankingTest` (3 giá trị phân biệt, 2 giá trị pairwise tương thích ngược, tất cả bằng nhau, hoà tại max, giá trị thiếu, ít hơn 2 số, giá trị âm — đã revert-test bỏ case hoà-tại-max, xác nhận 6/7 fail đúng rồi khôi phục) + 3 instrumented `CompareActTest` (mặc định 2 cột + cột 3 `GONE` ở mọi hàng — regression guard, toggle bật/tắt cột 3 đúng text+visibility, ranking đúng cả 3 cột với bộ ba H/He/Li thật).
- Verify UI: 2 screenshot thật (base64 qua logcat) — trạng thái 2 cột mặc định (không đổi so với trước) và trạng thái 3 cột sau khi bấm "+" (đủ 3 picker, bảng 3 cột, màu ranking đúng kể cả trường hợp He thiếu electronegativity "---").
- Full suite: `testDevDebugUnitTest` xanh. Instrumented: 3/3 pass trên emulator-5554, 3/3 pass trên **Samsung S24 Ultra thật (SM-S928B, serial R5CX613VZBR)** — device ưu tiên nhất theo yêu cầu ban đầu, kết nối lại đúng lúc cần smoke test feature cuối cùng vòng 4.
- Tự chấm điểm: **9.5/10** — tách `CompareRanking` pure tổng quát hoá đúng cho N giá trị thay vì hard-code riêng case-3, giữ logic 2-cột cũ nguyên vẹn từng dòng nên regression-safe tuyệt đối (không phải "nên giống" mà là "chính xác cùng code path"), UI toggle mặc định ẩn không phá trải nghiệm quen thuộc, smoke test thật trên đúng S24U ưu tiên nhất. Trừ điểm nhỏ vì không có test cho trường hợp cả 3 giá trị đều thiếu/không phải số (edge case ít quan trọng, không ảnh hưởng UI theo review lại code — `numeric.size < 2` đã cover).

**Tổng kết Vòng 4 (2026-09-02)**: cả 4 feature (Phát âm nguyên tố TTS, Nguyên tố xem gần đây, Category/Group Practice Mode, So sánh 3+ nguyên tố) đã hoàn tất theo đúng thứ tự effort tăng dần, mỗi feature đều pass gate >9/10 (9.4, 9.3, 9.4, 9.5) và đã push lên `origin/dev`.

# Vòng 5 (2026-09-02) — 4 feature mới chọn qua AskUserQuestion

User chọn cả 4 idea được đề xuất (Lọc nguyên tố nâng cao — recommended, Cỡ chữ/Accessibility, Reset tiến trình học tập, Widget câu hỏi nhanh). Yêu cầu rõ: **chỉ rã task vào file này, chưa code**. Thứ tự dưới đây xếp theo effort tăng dần dựa trên khảo sát thực tế code (không phải ước lượng ban đầu ở bước AskUserQuestion — thứ tự đã điều chỉnh sau khi đọc code thật, xem mục 19 để biết lý do "lọc nâng cao" bị đẩy lên effort cao hơn dự kiến ban đầu).

## 17. Reset tiến trình học tập — ✅ Đã fix (2026-09-02)

- Khảo sát: 4 nguồn dữ liệu "tiến trình" cần xoá, **không nguồn nào có sẵn method clear/reset**:
  - `feature/streak/StudyStreakPref.kt` (file `Study_Streak_Preference`, keys `current_streak`/`last_study_epoch_day`) — có `restore(streak, epochDay)` nhưng đó là hàm GHI giá trị (dùng cho Backup/Restore), không phải reset; dùng lại `restore(0, 0)` hoặc thêm `clear()`.
  - `feature/flashcard/FlashcardPref.kt` (file `Flashcard_Preference`, key phẳng theo từng symbol `flashcard_ease_$symbol`/`flashcard_interval_$symbol`/...) — chưa có bulk-clear, cần thêm (file riêng, không chứa dữ liệu nào khác nên `preference.edit { clear() }` an toàn).
  - `feature/exam/ExamHistoryPref.kt` — đã có sẵn `replaceHistory(emptyList())`, dùng thẳng, không cần thêm method mới.
  - `feature/quiz/QuizBestScorePref.kt` (file `Quiz_Best_Score_Preference`, key `best_score`) — `recordScore()` chỉ tăng dần, chưa có cách giảm về 0, cần thêm method mới.
- **Xác nhận quan trọng qua đọc code**: `feature/badge/BadgeCalculator.kt` **không lưu trạng thái badge riêng** — `computeUnlockedBadges()` tính on-the-fly từ 4 nguồn trên (`StudyStreakPref`/`ExamHistoryPref`/`QuizBestScorePref`/`FlashcardPref.countReviewedSymbols()`). Nghĩa là reset đúng 4 pref trên tự động reset badge, không cần xoá gì thêm cho Badge.
- **Bắt buộc KHÔNG được đụng**: `feature/vip/VipPrefs.kt` (đã có sẵn `clearGrantedAtMs()`/`clearUserRedeemed()` — không được gọi từ luồng reset progress, VIP là entitlement trả phí/redeem, không phải "tiến trình học tập").
- UI: 1 row mới trong `SettingsAct` (đặt gần khu Export/Import Backup, `a_settings.xml` quanh dòng `exportProgressSettings`/`importProgressSettings`), theo đúng convention row 70dp đã dùng cho `clearCacheSettings` (`SettingsAct.kt` hàm `cacheSettings()`). Bắt buộc `MaterialAlertDialogBuilder` confirm dialog trước khi xoá (hành động không thể hoàn tác) — tái dùng pattern confirm đã có ở luồng Import Backup (mục 11).
- Quyết định cần làm rõ khi code:
  1. `feature/history/RecentlyViewedPref.kt` có tính là "tiến trình" cần xoá không? Nghiêng về KHÔNG (nó gần với `pref/SearchPref.kt` — tiện ích tìm kiếm/xem gần đây, không phải thành tích học tập) — nhưng cần chốt rõ, có thể hỏi AskUserQuestion nếu không tự tin.
  2. Thêm `clear()` riêng lẻ vào từng 3 pref còn thiếu, hay viết 1 `ProgressResetManager.resetAll(context)` gom cả 4 nguồn — nghiêng về `ProgressResetManager` để tránh rải rác danh sách "cái gì tính là tiến trình" ở nhiều nơi gọi.
  3. Sau khi reset, `MainAct`/`BadgeAct` đang mở có cần refresh ngay UI (streak indicator, badge lock state) không cần thoát app, hay chấp nhận chỉ đúng sau khi mở lại màn hình (đơn giản hơn, có thể chấp nhận cho v1).
- **Phát hiện thêm qua đọc code khi cài đặt**: `feature/backup/BackupData.kt` (định nghĩa "progress" của mục 11, cũ hơn) chỉ gồm `flashcards`/`currentStreak`/`examHistory`/`notes` — **thiếu `QuizBestScorePref`** (thêm sau, ở mục 9 Badges) và **có thêm Notes** (không có trong danh sách reset của mục này). Quyết định: Reset Progress dùng đúng 4 nguồn `BadgeCalculator` đọc (streak/flashcard/exam/best-score) — đầy đủ hơn định nghĩa cũ của Backup, và **không đụng Notes** vì đó là nội dung tự viết của user (giống lý do loại `RecentlyViewedPref` — cả 2 đều là tiện ích/nội dung, không phải thành tích học tập). Đây là bug tiềm ẩn có sẵn trong Backup/Restore (thiếu backup Quiz Best Score) — ngoài phạm vi mục này, không sửa.
- `feature/reset/ProgressResetManager.kt` (mới, `resetAll(context)`): gọi `StudyStreakPref.restore(0, 0)`, `FlashcardPref.clear()` (mới thêm), `ExamHistoryPref.replaceHistory(emptyList())` (đã có sẵn), `QuizBestScorePref.clear()` (mới thêm) — gom 1 chỗ duy nhất để tránh rải rác danh sách "cái gì tính là tiến trình".
- UI: row `resetProgressSettings` trong `a_settings.xml` (đúng convention 70dp, đặt sau Import Backup), `SettingsAct.kt` với `MaterialAlertDialogBuilder` confirm dialog (tái dùng đúng string `cancel`/`confirm` đã có).
- **Quyết định đã chốt**: KHÔNG refresh UI ngay lập tức sau reset (không gọi lại `refreshStudyStreak()`/badge lock state trên Activity khác) — chấp nhận cho v1, đúng như đề xuất "đơn giản hơn" trong khảo sát ban đầu; UI đúng khi mở lại màn hình.
- Test: 3 instrumented `ProgressResetManagerTest` (wipe đúng 4 nguồn về mặc định, không đụng Notes/VIP, không crash khi gọi trên state đã rỗng sẵn — đã revert-test bằng cách tạm bỏ dòng gọi `FlashcardPref.clear()`, xác nhận fail đúng `expected:<0> but was:<3>` rồi khôi phục) + 2 instrumented `SettingsActTest` (Cancel không xoá gì, Confirm xoá đúng progress nhưng giữ nguyên Notes).
- Full suite: `testDevDebugUnitTest` xanh. Instrumented: 9/9 pass trên **CPH1989 phần cứng thật** (2 lần liên tiếp, xen giữa là revert-test xác nhận bug thật bị bắt).
- Tự chấm điểm: **9.4/10** — phát hiện đúng 1 gap tiềm ẩn trong Backup/Restore cũ (thiếu Quiz Best Score) qua việc đối chiếu 2 định nghĩa "progress" khác nhau trong codebase, chọn đúng định nghĩa đầy đủ hơn cho feature mới; revert-test xác nhận test thật sự bắt được lỗi thiếu sót (không phải test hình thức). Trừ điểm vì quyết định không refresh UI ngay là đơn giản hoá có thể gây khó chịu nhẹ cho user (thấy streak cũ vẫn hiện tới khi thoát màn hình), và chưa test trên S24U (bị session khác chiếm dụng suốt phiên).
- Trạng thái: ✅ Đã fix (2026-09-02), đã push `origin/dev`.

## 18. Cỡ chữ / Accessibility — ✅ Đã fix (2026-09-02)

- Khảo sát nền tảng đã có: `util/LocaleHelper.kt` hàm `setLocale()` build `Configuration(context.resources.configuration)` (copy-constructor) rồi CHỈ sửa locale/layout-direction — **fontScale hệ thống được kế thừa nguyên vẹn, không bị đụng tới**. Có comment để lại từ bug đã fix trước đó (`FIX (P3)`): việc từng ép `fontScale = 1.0f` ở đây đã ĐÈ MẤT cài đặt "cỡ chữ" toàn hệ thống (Accessibility) của user mỗi lần đổi ngôn ngữ — **bài học bắt buộc phải nhớ khi code mục này**: feature mới KHÔNG được thay thế (`=`) giá trị `fontScale`, chỉ được NHÂN THÊM hệ số của app (`config.fontScale *= multiplier`) để tôn trọng cả 2 lớp cài đặt (hệ thống + app).
- Điểm chèn: `act/BaseAct.kt` hàm `attachBaseContext()` — nơi duy nhất gọi `LocaleHelper.applyLanguage(context)`, cùng điểm chèn cho font-scale override.
- Thiết kế: `pref/FontScalePref.kt` mới, copy đúng pattern `pref/ThemePref.kt` (1 file SharedPreferences, 1 key Int, 3 giá trị) — đề xuất enum Small(0.85x)/Default(1.0x)/Large(1.15x) thay vì cho user nhập số tự do (đơn giản, tránh giá trị cực đoan phá layout). UI: 1 panel chọn 3-way giống `themeSettings()`/`binding.themePanel` trong `SettingsAct.kt` (radio-icon rows `ic_radio_checked`/`ic_radio_unchecked`), không phải row đơn giản.
- Quyết định cần làm rõ khi code:
  1. Đổi fontScale trên Activity đang mở có cần gọi `recreate()` ngay (giống luồng đổi theme đã có `themeChangeHandler`) hay chỉ áp dụng từ lần mở Activity kế tiếp — nghiêng về cần `recreate()` để trải nghiệm mượt, tái dùng đúng cơ chế theme đã có.
  2. Test layout ở scale 1.15x trên các màn hình chữ dày đặc nhất (bảng so sánh `CompareAct`, bảng Quiz) để đảm bảo không tràn/cắt chữ — cần liệt kê rõ danh sách màn hình phải kiểm tra bằng screenshot thật trước khi báo xong.
- `pref/FontScalePref.kt` (mới, đúng pattern `ThemePref`): `SMALL=0`/`DEFAULT=1`/`LARGE=2`, mặc định `DEFAULT`. `util/FontScaleHelper.kt` (mới) — `multiplierFor(Int): Float` pure (0.85x/1.0x/1.15x) tách riêng khỏi `applyFontScale(context): Context` (phần chạm Android, nhân `context.resources.configuration.fontScale * multiplier` — **nhân, không ghi đè**, đúng bài học từ comment `FIX (P3)` trong `LocaleHelper.setLocale()`). Chain vào `BaseAct.attachBaseContext()` ngay sau `LocaleHelper.applyLanguage()`.
- UI: `view_font_scale_panel.xml` (mới, copy đúng cấu trúc `view_theme_panel.xml`: 3 hàng radio Small/Default/Large + Cancel), row `fontSizeSettings` mới trong `a_settings.xml` (ngay sau Themes). `SettingsAct.kt` — `fontScaleSettings()`/`updateFontScaleRadioButtons()`/`selectFontScale()` tái dùng đúng pattern `finish(); startActivity(intent)` sau 300ms fade-out mà `themeSettings()` đã dùng (không phải `recreate()`) để `attachBaseContext()` chạy lại với hệ số mới.
- Test: 4 JVM `FontScaleHelperTest` (`multiplierFor` đủ 3 mức + fallback giá trị lạ — đã revert-test đổi 0.85f thành 0.9f, xác nhận 1 test fail đúng rồi khôi phục) + 2 instrumented `FontScalePrefTest` (mặc định, lưu/đọc đúng) + 3 instrumented `FontScaleHelperTest` (mặc định trả về đúng cùng 1 Context — không wrap thừa; Large/Small nhân đúng vào `fontScale` **hệ thống thật của device**, không ghi đè — bằng chứng trực tiếp cho đúng bài học FIX(P3)) + 2 instrumented `SettingsActTest` (chọn Large lưu đúng pref có cleanup `pressBackUnconditionally()` vì chọn xong trigger relaunch Activity ngoài scope scenario — đúng pattern đã fix ở mục 14, Cancel không đổi gì).
- Verify UI: 1 screenshot thật ở mức Large trên toàn màn hình Settings — xác nhận không vỡ layout, các dòng mô tả dài wrap 2 dòng gọn gàng, không bị cắt chữ.
- Full suite: `testDevDebugUnitTest` xanh. Instrumented: 13/13 pass trên **Samsung S24 Ultra thật (SM-S928B)** — bao gồm bằng chứng nhân đúng vào `fontScale` hệ thống thật (không phải giả lập).
- Tự chấm điểm: **9.5/10** — tuân thủ nghiêm ngặt bài học từ bug cũ (nhân không ghi đè), tách pure decision (`multiplierFor`) khỏi phần Android JVM-test được, test instrumented chứng minh trực tiếp trên `fontScale` hệ thống thật của S24U (không chỉ verify qua giá trị giả định), tái dùng đúng pattern relaunch đã có sẵn của Theme thay vì phát minh cơ chế mới, phát hiện và fix trước 1 vấn đề tiềm ẩn tương tự mục 14 (Activity relaunch ngoài scope scenario) trước khi nó gây flaky test. Trừ điểm nhỏ vì chưa test relaunch giữa NHIỀU Activity đang mở cùng lúc (chỉ Activity hiện tại re-run attachBaseContext, các Activity khác trong back stack chỉ cập nhật khi mở lại — giới hạn chấp nhận được, giống hệt hành vi Theme đã có từ trước).
- Trạng thái: ✅ Đã fix (2026-09-02), đã push `origin/dev`.

## 19. Lọc nguyên tố nâng cao — 📋 Picked

- **Khảo sát làm thay đổi đáng kể phạm vi/effort so với ước lượng ban đầu**: bảng tuần hoàn hiển thị trên `MainAct` **KHÔNG phải RecyclerView** — nó là layout XML tĩnh (`a_main.xml`), mỗi ô là 1 view riêng với id sinh từ tên nguyên tố (ví dụ `hydrogen_btn`), tra bằng `resources.getIdentifier("${item.element}_btn", "id", packageName)` trong `MainAct.kt` (`setOnCLickListenerSetups()`). Cái tên "filter" hiện có trong code (`searchFilter()`) thực ra chỉ là bộ chọn CÁCH SẮP XẾP (số hiệu/độ âm điện/alphabet) cho danh sách kết quả tìm kiếm ẩn (`rvElement`, RecyclerView thật), KHÔNG đụng tới bảng chính. Bảng chính hiện **không có cơ chế ẩn/làm mờ ô nào cả**.
- Do đó có 2 hướng phạm vi rất khác nhau về effort, PHẢI chốt trước khi code (đề xuất dùng AskUserQuestion nếu không rõ):
  - (a) Filter áp dụng lên bảng chính tĩnh: cần lặp qua tối đa 118 `findViewById` để set `alpha`/`visibility` theo điều kiện lọc — code mới hoàn toàn, rủi ro hiệu năng nhỏ (118 lookup mỗi lần đổi filter) nhưng trải nghiệm trực quan nhất (thấy ngay trên bảng).
  - (b) Filter chỉ mở rộng overlay tìm kiếm đã có (`rvElement`/`mAdapter`) — tái dùng hạ tầng `filterList()` có sẵn, effort thấp hơn nhiều, nhưng user phải mở ô tìm kiếm mới thấy kết quả lọc (không trực quan bằng phương án a).
- Dữ liệu lọc: `model/Element.kt` chỉ có `electro` (Double) sẵn trong bộ nhớ, không cần Context — lọc theo độ âm điện làm được ngay. Lọc theo **khối lượng nguyên tử** phải qua `util/ElementWeightCache.getMass(symbol)` (đọc JSON, cần `ElementWeightCache.init(context)` gọi trước — cần xác nhận đã init sẵn ở đâu đó trong `MainAct`/`RoyApp` trước khi dùng). Lọc theo category tái dùng thẳng `feature/quiz/CategoryFilter.kt` (`filterElementsByCategory`, `ALL_CATEGORIES`) đã có sẵn từ mục 15, không cần viết lại.
- Thiết kế (giả định chọn phương án a — trực quan hơn, đúng kỳ vọng "lọc bảng tuần hoàn" của user): 1 dialog/bottom sheet mới (2 seekbar/range cho mass + electronegativity, checkbox/chip cho category) → hàm pure `feature/quiz/CategoryFilter`-style mới trong `feature/filter/ElementFilter.kt` (`matches(element, criteria): Boolean`) → áp dụng lên bảng bằng cách set `alpha = 0.25f` (không dùng `GONE`, giữ layout ổn định không bị nhảy vị trí) cho ô không khớp.
- Quyết định cần làm rõ khi code:
  1. Chốt phương án (a) hay (b) trước — đây là quyết định ảnh hưởng effort lớn nhất của cả mục này.
  2. Filter có persist qua việc thoát/mở lại `MainAct` không, hay reset mỗi lần mở lại (giống Category Practice Mode dialog stateless ở mục 15) — nghiêng về KHÔNG persist (đơn giản hơn, tránh user quên đã bật filter rồi thắc mắc sao bảng thiếu nguyên tố).
  3. Có cần nút "Xoá bộ lọc" hiển thị rõ ràng khi filter đang active không (tránh user bối rối khi thấy bảng bị mờ nhiều ô).
- Test dự kiến: JVM unit test cho `ElementFilter.matches()` (đủ case: chỉ mass, chỉ electronegativity, chỉ category, kết hợp cả 3, không điều kiện nào = match hết), instrumented test luồng mở filter → chọn điều kiện → xác nhận đúng tập ô bị mờ/ẩn, test xoá filter trả bảng về trạng thái đầy đủ.
- Trạng thái: 📋 Picked (chưa code)

## 20. Widget câu hỏi nhanh — 📋 Picked

- Khảo sát: `widget/ShortCommandWidget.kt` hiện tại chỉ hiển thị fact tĩnh (`ElementWeightCache.getFact(symbol)` — câu đầu tiên của mô tả JSON) + 1 vùng bấm DUY NHẤT mở `ElementInfoAct` (`setOnClickPendingIntent` trên `widgetRoot`, có comment giải thích lý do bind vào `widgetRoot` thay vì `flWidgetSearchBar` để tránh no-op im lặng trên API<31). **Không có sẵn dữ liệu câu hỏi trắc nghiệm nào cho "nguyên tố hôm nay"** — toàn bộ hạ tầng trivia hiện tại (`feature/trivia/`) chỉ là fact-string, không phải câu hỏi+đáp án.
- **Không có tiền lệ nào trong app cho nhiều vùng bấm độc lập trong 1 `RemoteViews`** (grep `setOnClickPendingIntent` = đúng 1 kết quả, ở widget này) — mỗi đáp án cần 1 `PendingIntent` riêng (request code riêng) mang theo index đáp án qua Intent extra, vì `RemoteViews` không giữ được state tại thời điểm bấm.
- Kích thước hiện tại `res/xml/short_command_widget_info.xml` (giống nhau ở cả 3 biến thể build): `minWidth=200dp`, `targetCellWidth=3`, `minHeight=110dp` — khá chật cho 3-4 nút đáp án, cần tăng lên (cả 3 file build variant phải sửa đồng bộ).
- Thiết kế: `feature/trivia/TriviaQuestionGenerator.kt` mới (pure, theo đúng pattern `feature/exam/MolarMassQuestionGenerator.kt`: `generate(random, factLookup): Question(elementName, choices)`) — câu hỏi dạng "nguyên tố nào có fact này?" (đưa fact, 4 lựa chọn tên nguyên tố) để tái dùng đúng `ElementOfDay.indexForDay()` + `ElementWeightCache.getFact()` đã có, không tạo nguồn "nguyên tố hôm nay" thứ 2 (bắt buộc theo đúng data hiện có, tránh lệch với Daily Trivia notification cùng ngày).
- Quyết định cần làm rõ khi code:
  1. Cách disambiguate nút bấm: 4 `PendingIntent` riêng biệt (đơn giản, ít rủi ro hơn) hay `RemoteViewsService`/`RemoteViewsFactory` dạng list (phức tạp hơn nhiều, không có tiền lệ) — nghiêng hẳn về 4 `PendingIntent` riêng.
  2. Phản hồi đúng/sai có cần cập nhật lại ngay giao diện widget (`appWidgetManager.updateAppWidget` gọi lại từ 1 `BroadcastReceiver` mới) hay chỉ mở `Toast`/`ElementInfoAct` — cần chốt trước vì ảnh hưởng có cần thêm receiver mới hay không.
  3. Widget đổi kích thước tối thiểu có phá bố cục người dùng đã ghim trên home screen thật không (kích thước cell home-screen launcher khác nhau) — cần test thật trên launcher thật (S24U/CPH1989), không chỉ emulator.
  4. Layout `layout-v31/view_short_command_widget.xml` (Material You) và `layout/` (fallback) đều cần thêm 4 nút — có nguy cơ lệch UI giữa 2 biến thể nếu chỉ sửa 1 file.
- Test dự kiến: JVM unit test cho `TriviaQuestionGenerator` (4 đáp án phân biệt, câu hỏi luôn có đáp án đúng nằm trong choices, không crash khi fact rỗng — cần fallback), instrumented/manual test luồng bấm từng nút đáp án trên widget thật (Espresso không test được RemoteViews trực tiếp — cần quy trình verify riêng qua `AppWidgetHostView` hoặc test thủ công có screenshot, ghi rõ giới hạn này khi code).
- Trạng thái: 📋 Picked (chưa code)

## Quy tắc thực hiện chung

- Mỗi mục xong: build xanh, chạy test liên quan, cập nhật status trong file này + `doc/feat.md` mục Picked/Implemented, commit + push origin/dev.
- Không thêm dependency mới (Gson đã có sẵn nếu cần, nhưng ưu tiên flat SharedPreferences key theo pattern `NotesPref` trước — chỉ dùng Gson khi dữ liệu thật sự có cấu trúc list/object như mục 8, không dùng cho dữ liệu giá trị đơn).
- Không thêm WorkManager — mọi lịch định kỳ dùng cơ chế Android có sẵn (`updatePeriodMillis`, hoặc check-on-app-open).
- **Test rigor bắt buộc mỗi feature** (yêu cầu user 2026-09-01): unit test JVM cho mọi pure logic (mọi nhánh/case), instrumented test cho UI/edge-case, integration test luồng end-to-end thật, smoke test THẬT trên device đang connect (ưu tiên Samsung S24 Ultra — serial `R5CX613VZBR` — vì TECNO KJ7 hay bị app `com.galaxyjoy.cpuinfo` từ 1 session Claude Code khác dùng chung máy cướp focus gây false-fail, luôn kiểm tra `dumpsys window | grep mCurrentFocus` trước khi kết luận fail nào là bug thật). UI mới PHẢI chụp screenshot thật (base64-log-qua-logcat, xem cách làm ở mục 3/4) để tự verify trước khi báo xong, không chỉ đọc code. Tự audit + chấm điểm /10 sau mỗi feature — chỉ push nếu >9/10.
