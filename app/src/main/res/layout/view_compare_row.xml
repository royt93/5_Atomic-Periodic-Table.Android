<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/compareRowRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">

        <!-- Property Label -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvCompareLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="2.2"
            android:paddingStart="12dp"
            android:paddingEnd="4dp"
            android:textColor="?attr/colorOnSecondaryContainer"
            android:textSize="12sp"
            android:textStyle="bold" />

        <!-- Value for Element 1 -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvCompareVal1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1.5"
            android:gravity="center"
            android:textColor="?attr/colorOnSurface"
            android:textSize="12sp" />

        <!-- Indicator: ▲ ▼ = -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvCompareIndicator"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="0.5"
            android:gravity="center"
            android:textSize="12sp"
            android:textStyle="bold" />

        <!-- Value for Element 2 -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvCompareVal2"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1.5"
            android:gravity="center"
            android:paddingEnd="8dp"
            android:textColor="?attr/colorOnSurface"
            android:textSize="12sp" />

        <!-- Value for Element 3 (mục 16: 3-way compare) — GONE unless a 3rd element is active -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvCompareVal3"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1.5"
            android:gravity="center"
            android:paddingEnd="8dp"
            android:textColor="?attr/colorOnSurface"
            android:textSize="12sp"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Subtle bottom divider — uses theme-aware color for light/dark mode -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="?attr/colorOutline"
        android:alpha="0.2" />

</LinearLayout>

